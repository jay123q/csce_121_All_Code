// string streams take 4 bits
// cin.clear() takes the users input


// if it reaches the fourth bit it outputs
//
//
// Image file format is the w =  nummber of columns  h = number of rows
//string type;
// ifs >> type;
/* int w = 0; int h = 0;
int num 
ifs >> num;

for (int row = 0; row < h; ++row){
    for(int col = 0; col < w; ++col){
        int r = 0; int g= 0, int b = 0;

    }


}

now rework row by column to convert to standarrd image


to convert to grayscale take the average of all the values


for (int row = 0; row <  height; ++row){
    for(int col =0; col<width; ++col){
        int bwColor([col][row]) = image[col][row].r + image[col][row].g+ image[col][row].p
        image[col][row] = {bwColor,bwColor, bwColor}
    }
    
}